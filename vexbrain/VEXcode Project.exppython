{"mode":"Text","hardwareTarget":"brain","textContent":"#region VEXcode Generated Robot Configuration\nfrom vex import *\nimport urandom\nimport time\n\n# Brain should be defined by default\nbrain = Brain()\n\n# Robot configuration code\nbrain_inertial = Inertial()\nx_axis_motor = Motor(Ports.PORT3, True)\n\n\n# Wait for sensor(s) to fully initialize\nwait(100, MSEC)\n\n# generating and setting random seed\ndef initializeRandomSeed():\n    wait(100, MSEC)\n    xaxis = brain_inertial.acceleration(XAXIS) * 1000\n    yaxis = brain_inertial.acceleration(YAXIS) * 1000\n    zaxis = brain_inertial.acceleration(ZAXIS) * 1000\n    systemTime = brain.timer.system() * 100\n    urandom.seed(int(xaxis + yaxis + zaxis + systemTime)) \n\n# Initialize random seed \ninitializeRandomSeed()\n\n#endregion VEXcode Generated Robot Configuration\n# ------------------------------------------\n# \n# \tProject:      VEXcode Project\n#\tAuthor:       VEX\n#\tCreated:\n#\tDescription:  VEXcode EXP Python Project\n# \n# ------------------------------------------\n\n# Library imports\nfrom vex import *\n\nx_axis_motor.set_velocity(1,PERCENT)\n\ninertial_sensor = Inertial()\n\n# Function to reset heading and align motor to face the front\ndef reset_heading_and_align_motor_right():\n    # Reset the inertial sensor's heading to 0 degrees (robot's front)\n    inertial_sensor.reset_rotation()\n\n    # Reset the motor encoder to start from a known position\n    x_axis_motor.reset_position()\n\n    # Rotate motor to the front position (aligned to the 0 degrees heading)\n    x_axis_motor.spin_for(REVERSE, 90, DEGREES)  # Adjust this based on your robot setup\n    \n\n# Call the function to reset heading and align the motor to the front\nreset_heading_and_align_motor_right()\n\ntime.sleep(3)\nbrain.screen.clear_screen()\n\ndef move_right(amount):\n    x_axis_motor.spin_for(FORWARD,amount,DEGREES)\n\ndef move_left(amount):\n    x_axis_motor.spin_for(REVERSE,amount,DEGREES)\n\nmove_right(30)\n\n# Begin project code\n","textLanguage":"python","robotConfig":[{"port":[3],"name":"x_axis_motor","customName":true,"deviceType":"Motor","deviceClass":"motor","setting":{"reversed":"true","fwd":"forward","rev":"reverse"}}],"slot":0,"platform":"EXP","sdkVersion":"20240802.15.00.00","appVersion":"4.0.8","fileFormat":"2.0.0","targetBrainGen":"First","v5SoundsEnabled":false}